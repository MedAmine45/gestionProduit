@model projetgestionproduit.Models.Produit

@{
    ViewBag.Title = "Saisie d'un produit";
}
<br />
<div class="container col-md-8 col-md-offset-2">
    <div class="panel panel-primary">
        <div class="panel-heading">Saisie d'un produit</div>
        <div class="panel-body">
            @using (Html.BeginForm("SaveProduit", "Produit", FormMethod.Post))
            {
                <div class="form-group">
                    <label class="control-label">Désignation:</label>
                    @Html.TextBoxFor(p => p.Designation, new { @class = "form-control" })
                    @Html.ValidationMessageFor(p => p.Designation, "", new { @class = "text-danger" })
                </div>
                <div class="form-group">
                    <label class="control-label">Prix:</label>
                    @Html.TextBoxFor(p => p.Prix, new { @class = "form-control" })
                    @Html.ValidationMessageFor(p => p.Prix, "", new { @class = "text-danger" })
                </div>

                <div class="form-group">
                    <label class="control-label">Quantité:</label>
                    @Html.TextBoxFor(p => p.Quantite, new { @class = "form-control" })
                    @Html.ValidationMessageFor(p => p.Quantite, "", new { @class = "text-danger" })
                </div>
                <div class="form-group">
                    <label class="control-label">Catégorie:</label>
                    @*<select name="CategorieID" class="form-control">
                            @{
                                foreach (var c in ViewBag.Categories)
                                {
                                    <option value="@c.CategorieID">@c.NomCategorie</option>
                                }
                            }
                        </select>*@
                    @Html.DropDownList("CategorieID", (SelectList)ViewBag.Categories,"Choisir une categorie", new { @class = "form-control" })

                </div>

                <div>
                    <button type="submit" class="btn btn-primary"> Save</button>
                    @if (TempData["message"] != null)
                    {
                        <div style="color:rgb(255, 0, 0);">
                            @Html.Raw(TempData["message"])
                        </div>
                    }
                    @*@Html.ActionLink("Save", "SaveProduit",null, new { @class = "btn btn-primary" })*@
                </div>

            }
        </div>
    </div>
</div>


